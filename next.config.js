/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Domains of images being used
    domains: [
      "thrangra.sirv.com",
    ]
  }
}

module.exports = nextConfig
